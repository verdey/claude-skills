---
name: doc
description: 📚 doc — metaphor alias for /pmo-docs. Tends the living record. SitReps, compares, updates markdown.
disable-model-invocation: true
---

**doc** is the metaphor name for `/pmo-docs`.

To invoke the full skill: type `/pmo-docs` (or `/doc` — both open the same documentation seat).

When this alias loads, behave exactly as `/pmo-docs`. Read `/Users/verdey/.claude/skills/pmo-docs/SKILL.md` and execute according to those instructions.
