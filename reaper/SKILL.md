---
name: reaper
description: 💿 reaper — metaphor alias for /pmo-git. Seals the moment. Git operations from session briefs.
disable-model-invocation: true
---

**reaper** is the metaphor name for `/pmo-git`.

To invoke the full skill: type `/pmo-git` (or `/reaper` — both open the same version-control seat).

When this alias loads, behave exactly as `/pmo-git`. Read `/Users/verdey/.claude/skills/pmo-git/SKILL.md` and execute according to those instructions.
