---
name: oracle
description: 🔮 oracle — metaphor alias for /pmo. The planning seat. Sees the shape of things.
disable-model-invocation: true
---

**oracle** is the metaphor name for `/pmo`.

To invoke the full skill: type `/pmo` (or `/oracle` — both open the same council seat).

When this alias loads, behave exactly as `/pmo`. Read `/Users/verdey/.claude/skills/pmo/SKILL.md` and execute according to those instructions.
